<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Needed to set ringer mode to silent/vibrate (not needed for normal) -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <!-- Needed to mute/restore media (music/video) volume alongside ringer mode -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- Needed for precise alarms on Android 12+ -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- Re-schedule alarms after phone restarts (AlarmManager clears them on reboot) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Lets us prompt the user to exempt this app from battery optimization,
         so OEMs (Xiaomi, Samsung, OnePlus, etc.) don't kill the scheduled alarms -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_custom"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_custom"
        android:theme="@style/Theme.AutoSilent"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".JntuaActivity"
            android:label="JNTUA Schedule"
            android:parentActivityName=".MainActivity" />

        <!-- Fires at work start / work end times to flip ringer mode -->
        <receiver
            android:name=".AlarmReceiver"
            android:exported="false" />

        <!-- Re-schedules the daily alarms after a reboot -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".QuickSilenceReceiver"
            android:exported="false" />

        <!-- Quick Settings tile: manual override, e.g. "unsilence early today" -->
        <service
            android:name=".SilentTileService"
            android:icon="@drawable/ic_tile_silent"
            android:label="@string/tile_label"
            android:exported="true"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.ACTIVE_TILE"
                android:value="true" />
        </service>

    </application>
</manifest>
